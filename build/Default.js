// Imports
import Gulp from 'gulp';
import TaskList from 'gulp-task-listing';

/**
 * Default
 * the default gulp task. List our all tasks
 *
 * @uses {gulp, gulp-task-listing}
 */
Gulp.task('default', TaskList);
